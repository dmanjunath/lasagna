/** Full-screen loading overlay — currently disabled (no loading indicators). */
export function LoadingScreen({ visible }: { visible: boolean }) {
  return null;
}

/** Inline loading fallback for lazy-loaded pages — currently disabled. */
export function PageLoader() {
  return null;
}
