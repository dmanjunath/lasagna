---
import { APP_URL, GITHUB_URL } from "../config";

const layers = [
  { idx: "01", label: "Insurance deductible", val: "$2,500 / $2,500", cls: "l1" },
  { idx: "02", label: "Emergency fund", val: "$18,400 / $24,000", cls: "l2" },
  { idx: "03", label: "Employer match", val: "capturing 100%", cls: "l3" },
  { idx: "04", label: "High-interest debt", val: "$0 · paid off", cls: "l4" },
  { idx: "05", label: "Roth IRA", val: "$4,200 / $7,000", cls: "l5" },
  { idx: "06", label: "Surplus investing", val: "— not yet", cls: "l6" },
];
---

<section class="hero">
  <div class="hero-text">
    <div class="eyebrow" style="margin-bottom: 20px;">Personal finance, layered</div>
    <h1 class="serif">
      Your money.<br />
      Your <em>vault</em>.<br />
      Your AI.
    </h1>
    <p class="hero-sub">
      LasagnaFi is a personal financial dashboard with a private AI advisor — built on a simple premise: your data never leaves your vault. The model gets numbers, never names.
    </p>
    <div class="hero-cta">
      <a href={`${APP_URL}/login`} class="btn btn-primary">Get started free →</a>
      <a href="#privacy" class="btn btn-ghost">Read the privacy model</a>
    </div>
    <div class="hero-meta">
      <span>Self-hosted or managed</span>
      <span>Open source core</span>
      <span>No data brokers, ever</span>
    </div>
  </div>

  <div class="stack-viz">
    {layers.map((l) => (
      <div class={`stack-layer ${l.cls}`}>
        <div class="layer-left">
          <span class="idx">{l.idx}</span>
          <span class="lbl">{l.label}</span>
        </div>
        <span class="val">{l.val}</span>
      </div>
    ))}
    <div class="stack-caption">FIG. 01 — YOUR PERSONAL FINANCIAL LAYERS</div>
  </div>
</section>

<style>
  .hero {
    max-width: 1240px;
    margin: 0 auto;
    padding: 80px 32px 64px;
    display: grid;
    grid-template-columns: 1.1fr 1fr;
    gap: 64px;
    align-items: center;
  }

  h1 {
    font-size: clamp(54px, 6.5vw, 96px);
    line-height: 0.98;
    letter-spacing: -0.022em;
    font-weight: 400;
  }

  h1 em {
    font-style: italic;
    color: var(--sauce);
  }

  .hero-sub {
    margin-top: 24px;
    font-size: 18px;
    line-height: 1.55;
    color: var(--ink-soft);
    max-width: 500px;
  }

  .hero-cta {
    display: flex;
    gap: 12px;
    margin-top: 32px;
    flex-wrap: wrap;
  }

  .hero-meta {
    margin-top: 28px;
    display: flex;
    gap: 24px;
    flex-wrap: wrap;
    font-family: var(--mono);
    font-size: 11px;
    color: var(--muted);
  }

  .hero-meta span::before {
    content: "— ";
    color: var(--sauce);
  }

  /* Stack viz */
  .stack-viz {
    position: relative;
    display: flex;
    flex-direction: column;
    gap: 9px;
    padding: 28px 0;
  }

  .stack-layer {
    border-radius: 12px;
    padding: 16px 20px;
    display: flex;
    align-items: center;
    justify-content: space-between;
    box-shadow: 0 1px 0 rgba(0,0,0,0.04), 0 12px 24px -16px rgba(31,26,22,0.3);
    transform-origin: left center;
    transition: transform 0.25s ease;
    cursor: default;
  }

  .stack-layer:hover { transform: translateX(6px); }

  .layer-left {
    display: flex;
    align-items: center;
    gap: 12px;
  }

  .idx {
    font-family: var(--mono);
    font-size: 10px;
    opacity: 0.65;
  }

  .lbl { font-size: 13px; font-weight: 500; }
  .val { font-family: var(--mono); font-size: 12px; font-variant-numeric: tabular-nums; }

  /* Layer colors */
  .l1 { background: var(--sauce);    color: #FBF6EC; width: 94%; }
  .l2 { background: var(--cheese);   color: var(--ink); width: 88%; margin-left: 6%; }
  .l3 { background: var(--noodle);   color: var(--ink); width: 92%; margin-left: 2%; }
  .l4 { background: var(--basil);    color: #FBF6EC; width: 86%; margin-left: 8%; }
  .l5 { background: var(--crust);    color: #FBF6EC; width: 90%; margin-left: 4%; }
  .l6 { background: var(--burgundy); color: #FBF6EC; width: 96%; }

  .stack-caption {
    position: absolute;
    bottom: -2px;
    left: 0;
    font-family: var(--mono);
    font-size: 10px;
    color: var(--muted);
    letter-spacing: 0.08em;
  }

  @media (max-width: 860px) {
    .hero {
      grid-template-columns: 1fr;
      padding: 48px 20px 48px;
      gap: 40px;
    }
    .stack-viz { padding-bottom: 24px; }
  }
</style>
