/** Documented modeling constants for the deterministic schedule (v1). Surfaced
 *  in ScheduleSection.assumptions so the report can disclose them. */
export const RE_APPRECIATION = 0.03;          // annual real-estate appreciation
export const DIVIDEND_YIELD = 0.018;          // assumed qualified-dividend yield on taxable equity
export const DEFAULT_COST_BASIS_RATIO = 0.65; // used when holdings carry no cost basis
